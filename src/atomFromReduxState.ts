const atomFromReduxState = <StateType = any>(namespace: string): StateType => {
  // @TODO
  console.log('atomFromReduxState()', namespace);
  return null as any;
};

export default atomFromReduxState;
