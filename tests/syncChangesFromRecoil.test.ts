/* eslint-env jest */

import syncChangesFromRecoil from '../src/syncChangesFromRecoil';

describe('TODO', () => {
  it('@TODO', () => {
    const sampleSelector = syncChangesFromRecoil((state) => state);

    console.log('@TODO', sampleSelector);
  });
});
