/* eslint-env jest */

import selectorFromReselect from '../src/selectorFromReselect';

describe('TODO', () => {
  it('@TODO', () => {
    const sampleSelector = selectorFromReselect((state) => state.whatever);

    console.log('@TODO', sampleSelector);
  });
});
